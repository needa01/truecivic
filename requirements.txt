# Core dependencies
httpx>=0.25.0
requests>=2.31.0
python-dotenv>=1.0.0

# Data processing
pandas>=2.1.0
pydantic>=2.4.0

# Database (will add when ready)
# psycopg[binary]>=3.1.0
# asyncpg>=0.29.0
# sqlalchemy>=2.0.0
# alembic>=1.12.0

# Redis (will add when ready)
# redis>=5.0.0

# API (will add when ready)
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# strawberry-graphql>=0.209.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Development
ruff>=0.1.0
black>=23.10.0
mypy>=1.6.0
